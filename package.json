{
  "private": true,
  "name": "atk",
  "version": "0.0.0",
  "scripts": {
    "prepare": "pnpm -r postinstall",
    "prepare-workspace": "pnpm -r postinstall"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "config-prettier": "workspace:*",
    "eslint-formatter-pretty": "^6.0.1",
    "plop": "^4.0.1",
    "typescript": "^5.4.5"
  },
  "prettier": "config-prettier"
}
